# Fonts — Mars & Moon

Fonts are currently loaded via `next/font/google` in `app/layout.tsx`
(Tenor Sans, Cormorant Garamond, Inter) — self-hosting is optional.

To self-host instead, drop the WOFF2 files here and swap the `next/font/google`
imports for `next/font/local` declarations pointing at these files.
